name: Scheduled Workflow

on:
  workflow_dispatch:
  schedule:
    - cron: '*/1 * * * *'  # Runs every minute

jobs:
  print_time:
    runs-on: ubuntu-latest
    steps:
    - name: Print current time
      run: date
